import React from 'react';

const LoginIntro = () => {
  return (
    <div className='lead text-primary mt-3'>
      <p>This is a Login Form app built with React and Redux as well as bootstrap.</p>
      <p>It fetches data from the mock API.</p>
    </div>
  );
};

export default LoginIntro;
